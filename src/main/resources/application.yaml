spring:
  application:
    name: api_rest_mysql
  datasource:
    url: jdbc:mysql://localhost:3306/project_manager_db?useSSL=false&serverTimezone=UTC
    username: username
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  security:
    user:
      name: admin
      password: admin123